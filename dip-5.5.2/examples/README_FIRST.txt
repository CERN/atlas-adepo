To run the DIP client/Server examples:-

1. Build both the client and the server (windows - using provided projects, linux - make files provided, use make -f <makeFileName>)
2. Make sure the shared libraries can be found for both the client and the server. 
